# Documentation build requirements (used by Read the Docs).
# Kept minimal and pinned to majors: every dependency here must be one the docs actually use.
sphinx>=7,<9
myst-parser>=2,<5
sphinx-rtd-theme>=2,<4
sphinx-copybutton>=0.5
sphinx-design>=0.5
